services:
  - type: web
    name: studybuddy
    runtime: node
    plan: free
    buildCommand: npm install && npm run build && npx drizzle-kit push
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: GEMINI_API_KEY
        sync: false
